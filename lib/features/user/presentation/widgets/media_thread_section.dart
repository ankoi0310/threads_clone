import 'package:flutter/material.dart';

class MediaThreadSection extends StatelessWidget {
  const MediaThreadSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Bạn chưa đăng thread nào có file phương tiện.'),
    );
  }
}
