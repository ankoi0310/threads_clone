import 'package:flutter/material.dart';

class ReplyThreadSection extends StatelessWidget {
  const ReplyThreadSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Bạn chưa trả lời thread nào'));
  }
}
